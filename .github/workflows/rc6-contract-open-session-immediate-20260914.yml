name: RC6 contractual auth diagnosis 2026-09-14

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "STATE=PAUSED_PENDING_ONE_SHOT_AUTH_DIAGNOSTIC"
